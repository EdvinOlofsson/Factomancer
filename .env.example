DISCORD_TOKEN=your_bot_token_here
CLIENT_ID=your_application_client_id
GUILD_ID=your_test_server_guild_id
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Rate limiting (optional — these are the defaults)
# RATE_LIMIT_PER_USER_HOUR=5    # max /factcheck calls per user per hour
# RATE_LIMIT_GLOBAL_DAILY=20    # max /factcheck calls across all users per day (~$1.40 max/day)
